@model Verificador.Vista.Models.ViewModels.UsuarioViewModel
@{
    ViewBag.Title = "Recuperar contraseña";
}
<script>


</script>
@section featured {

}
<div class="container">
    <div id="loginbox" style="margin-top: 50px;" class="mainbox col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
        <div class="panel panel-info">
            <div class="panel-heading">
                <div class="panel-title">Recuperar Contraseña</div>
                <div style="float: right; font-size: 80%; position: relative; top: -10px"><a href='@Url.Content("~/Login")'>Ingreso al Sistema</a></div>
            </div>

            <div style="padding-top: 30px" class="panel-body">

                <div style="display: none" id="login-alert" class="alert alert-danger col-sm-12"></div>

                <form id="formRecuperar" class="form-horizontal" role="form" method="post" action='@Url.Content("~/Login/Recuperar")'>

                    <div style="margin-bottom: 25px" class="input-group">
                        <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
                            @(Html.Kendo().TextBox()
                                .Name("email")
                                .HtmlAttributes(new
                                {
                                    placeholder = "Email",
                                    required = "required",
                                    validationmessage = "Ingrese su email",                                                                        
                                    @class="form-control"
                                })
                            )
                    </div>                    
                    <div style="margin-top: 10px" class="form-group">
                        <div class="col-sm-12 controls">
                            <button class="btn btn-success" type="submit">Enviar</button>                            
                        </div>
                    </div>                   
                    @if (!string.IsNullOrEmpty(ViewBag.Error as string))
                    {        
                        <div class="alert alert-info">
                          <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                          <strong>@ViewBag.Error</strong>
                        </div>
                    }
                </form>



            </div>
        </div>
    </div>    
</div>


<script>

    $(function () {
        var validator = $("#formRecuperar").kendoValidator().data("kendoValidator");
        var status = $(".status");

        $("form").submit(function (event) {
            if (validator.validate()) {

            } else {
                event.preventDefault();
            }
        });
    });





</script>